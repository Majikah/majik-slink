// ─── Constants ────────────────────────────────────────────────────────────────

export const SLINK_VERSION = 1 as const;
export const CANONICAL_PREFIX = "majik-slink-v1:" as const;
export const CODE_PREFIX = "majik-slink:" as const;
export const CODE_HEX_LENGTH = 32 as const; // chars from the 64-char SHA-256 hex digest
